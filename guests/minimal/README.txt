Code here will run in the unprivileged mode of the processor, 
that is run as a "Guest" on top of the hypervisor.

/Minimal
Written to test the basic functions of the hypervisor: Exception handlers,
basic context switching etc. Guest provides its own uart soc driver.